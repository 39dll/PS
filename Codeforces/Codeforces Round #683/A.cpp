#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0); cin.tie(0);
	int T; cin >> T;
	while (T--) {

		int N;
		cin >> N;
		cout << N << '\n';
		for (int i = 0; i < N; i++) {
			cout << i + 1 << ' ';
		}cout << '\n';

	}
}
