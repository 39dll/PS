#include <bits/stdc++.h>
#include <unordered_set>
#include <unordered_map>
#include <ext/rope>
#pragma GCC optimize("O3")
#define DEBUG 1
#define dout if(DEBUG) cout
#define fuck if(DEBUG)cout.flush(),system("pause"); return 0
using namespace std;
using namespace __gnu_cxx;
typedef long long ll;
typedef pair<ll, ll> pll;
int main(){
    if(!DEBUG)ios::sync_with_stdio(0), cin.tie(0);
    
    fuck;
}
